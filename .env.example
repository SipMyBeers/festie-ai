# Ollama (running on Mac Mini via Cloudflare Tunnel)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=festie-coachella

# Twilio (phone number for SMS webhook — no auth needed, Twilio POSTs to /api/sms)
TWILIO_PHONE_NUMBER=+18775092803

# Stripe (set to enable paid guide access)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_ENABLED=false
